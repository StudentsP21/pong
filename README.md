# pong

Поле
-ширина(константа) 61 символов, 
-высота(константа) 30 символов.

(мячик) 
-координата x
-координата y
- направление (отдельный enum)
-скорость

перечисление(enum) того какой игрок

для обоих игроков: 
- cчёт 
- координата y 

константа скорость 

константа размер ракетки

константа FPS
(Виталий)

Основной цикл игры(функция)
- счётчик циклов
- счётчик секунд(функция) на основе количества циклов и FPS
- паузу, основываясь на fps
- при попадании добавить очков нужному игроку
- при попадании запустить сначала
- при наборе необходимого количество очков(константа = 3) останавливать игру, писать кто победил
- 
(Айдар)

Функция проверки попадания мяча на границы, увеличение счёта нужного игрока(Лев)
Функция рассчёта стороны движения и движения для искуственного интеллекта(Артём)
Функция рассчёта движения для мяча и движения(Руслан)

Нужен enum для значений клавиш(ARROW_UP, ARROW_DOWN) - если не найдешь
Функция для проверки ввода(возвращает направление), функции для движения ракетки(принимает направление)(Ильяс)

Функция отрисовки поля(собирая текст в массив, а потом выводя целиком)
Функция очистки поля
Функция проверки совпадения положения и отрисовки мяча в случае совпадения
Функция проверки совпаденгия и отрисовки одной точки ракетки при совпадении
(Камиль)
